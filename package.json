{
  "name": "sap-architect-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Whiteboard → SAP architecture Draw.io — SAP CAP (CAPM) backend",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:cap": "npm run dev -w @sap-architect/cap",
    "dev:web": "npm run dev -w @sap-architect/web",
    "build": "npm run build -w @sap-architect/shared && npm run build -w @sap-architect/drawio && npm run build -w @sap-architect/core && npm run build -w @sap-architect/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "node --test test.mjs",
    "demo:pipeline": "npm run demo:pipeline -w @sap-architect/core",
    "corpus:seed": "npm run corpus:seed -w @sap-architect/core",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}
